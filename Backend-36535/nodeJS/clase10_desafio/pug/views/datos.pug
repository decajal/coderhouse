html
  head
    meta(charset='UTF-8')
    meta(http-equiv='X-UA-Compatible' content='IE=edge')
    meta(name='viewport' content='width=device-width, initial-scale=1.0')

    link(href='https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css' rel='stylesheet')
    title Productos
  body
    .container-sm
      h1.text-primary Vista de Productos
      if tieneElementos
        table.table
          thead
            tr
              th(scope='col') #
              th(scope='col') Nombre
              th(scope='col') Precio
              th(scope='col') Foto
          tbody
          each x in productos
            tr
              th(scope='row')=x.id
              td=x.title
              td=x.price
              td=x.thumbnail
      else
        .alert.alert-primary(role='alert') No se encontraron productos.
      p.text-end
        a(href='/') Ir al formulario de Ingreso de Productos

    script(src='https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/js/bootstrap.bundle.min.js')
