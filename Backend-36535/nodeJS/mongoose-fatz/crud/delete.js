require("../connection");
const Product = require("../models/Product");

const someFunction = async () => {
  //const result = await Product.deleteMany({ name: "keyboard" }); // result es un informe de las coincidencias y lo eliminado
  //const result = await Product.deleteOne({ name: "keyboard" }); // result es un informe de las coincidencias y lo eliminado
  //const result = await Product.findOneAndDelete({ name: "keyboard" }); // en este caso result es el dato eliminado
  //const result = await Product.findByIdAndDelete("62ce0668305780b10fab8967"); // emn este caso result es el dato eliminado
  console.log(result);
};

someFunction();
